// make route for /api/notes app.get()
// have inspector up in chrome and check console for errors
// don't change any front end stuff, just express functionality (the routes)
// deploy assignment to heroku


app.get('/api/notes', function(req,res) {
    //fs.readfile and read in that db.json
    //res.json(data) that db.json file
})






//  fsreadFile and fswriteFile